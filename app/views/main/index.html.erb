<%
=begin%>
 Na pasta "layouts", dentro da pasta "view", existe um arquivo chamado "application.html.erb", o qual recebe todos os parâmetros que precisam ser assocuados ao header da página html 
<%
=end%>

<div class="d-flex align-itens-center justify-content-center">
    <h1> Welcome to Schedule Tweets </h1>
</div>

<% if @users %>
    <h2>Users</h2>
<% end %>

<div id="users">
    <% @users.each do |user| %>
        <%= render user %>
    <% end %>
</div>
